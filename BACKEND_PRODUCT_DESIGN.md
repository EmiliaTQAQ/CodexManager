# Codex 模型管理窗口 — 后端产品设计文档

> 版本：MVP
> 更新时间：2026-09-07

## 1. 产品定位

后端是一个运行在用户本机的轻量配置管理服务，为 Codex 模型与 Provider 管理窗口提供可靠的数据和文件操作能力。

产品最终以独立 macOS `.app` 运行。前端页面作为应用内置资源加载到 WebView，不通过系统浏览器访问；Python 后端随应用自动启动和退出，用户不需要单独运行任何服务。

它不负责聊天和远程账号体系，主要解决以下问题：

- 统一管理多个 Provider 配置；
- 从真实的 Codex 配置文件读取当前生效状态；
- 安全地切换 Provider 和默认模型；
- 拉取、编辑和保存模型信息；
- 在配置写入失败或验证失败时自动恢复。

## 2. 整体方向

采用 **本地优先、单用户、服务化封装** 的设计：

```text
独立 macOS App
        |
        +-- pywebview 原生窗口
        |       |
        |       +-- 内嵌 HTML / CSS / JavaScript
        |
        | 同进程 JS Bridge
        v
Python 本地后端
        |
        +-- Provider 管理
        +-- Codex 配置读写
        +-- 模型拉取与验证
        +-- 备份与回滚
        +-- 密钥存储
        |
        +-- ~/.codex/config.toml
        +-- ~/.codex/auth.json
        +-- ~/.codex/models.json
```

前端和后端由同一个应用管理生命周期，优先通过 pywebview JS Bridge 通信，不依赖公开 HTTP 端口。前端不直接读取文件、不直接保存 API Key，所有敏感和具有副作用的操作都由后端完成。

HTML 页面只是桌面应用的界面实现方式，不作为可以独立打开的网页版产品交付。

## 3. 核心功能

### 3.1 Provider 管理

提供 Provider 的新增、编辑、删除和查询能力，主要信息包括：

- 名称和唯一 ID；
- `base_url`；
- `wire_api`；
- 默认模型；
- 启用模型列表；
- 模型目录及编辑后的元数据；
- 最近一次验证结果。

删除当前正在使用的 Provider 应被禁止，避免配置失去有效目标。

### 3.2 当前状态读取

后端启动和每次关键操作前，都从真实的 `~/.codex/config.toml` 解析：

- 当前 Provider；
- 当前模型；
- 当前配置是否完整可解析；
- 是否存在可用回滚备份。

`config.toml` 是当前生效状态的唯一来源。Provider 本地资料中的 `active_provider_id` 只能作为缓存，不能替代真实配置。

### 3.3 模型拉取与编辑

后端使用 Provider 的地址和密钥请求 `/v1/models`，将返回结果转换为统一的模型目录。

基本规则：

- 以模型 `slug` 作为唯一标识；
- 拉取时合并，不覆盖用户已经编辑过的显示名和元数据；
- 支持手动新增和删除模型；
- 仅将用户勾选的模型写入 `models.json`；
- Provider 返回异常时保留原模型目录，不产生部分更新。

### 3.4 Provider 验证

验证是独立操作，不修改当前 Codex 配置。后端至少需要识别：

- 请求成功；
- API Key 无效；
- Provider 不支持当前接口类型；
- 地址错误；
- 超时；
- 上游 `4xx` 或 `5xx`。

验证结果写入 Provider 的最近检查状态，并返回前端展示所需的简短错误信息。

### 3.5 安全启用与切换

启用 Provider 是后端最核心的业务流程：

1. 重新读取磁盘上的当前配置；
2. 校验目标 Provider 和默认模型；
3. 创建当前配置的完整备份；
4. 生成新的 `config.toml`、`auth.json` 和 `models.json`；
5. 使用临时文件和原子替换完成写入；
6. 重新解析写入结果；
7. 请求 Provider 进行验证；
8. 验证失败时自动恢复备份；
9. 成功后更新当前状态和最近验证结果。

任何一步失败，都不能留下半套新配置。

### 3.6 回滚

每次启用前自动生成一份带时间戳的备份，至少包含：

- `config.toml`；
- `auth.json`；
- `models.json`；
- 备份时间和原始配置摘要。

用户可以从界面执行“回滚上一步”。回滚完成后，后端重新读取配置并返回真实状态。

## 4. 数据存储

### 4.1 Provider 数据

MVP 阶段使用本地 JSON 保存非敏感 Provider 数据：

```text
~/Library/Application Support/CodexManager/providers.json
```

文件保存 Provider 信息、模型目录、最近验证结果和版本号，便于后续迁移数据格式。

### 4.2 API Key

API Key 优先使用 macOS Keychain 保存。JSON 中只保存密钥引用或“已配置”状态，不返回明文密钥。

如果 MVP 暂时使用文件存储，必须满足：

- 文件权限为 `0600`；
- 父目录权限为 `0700`；
- 查询接口只返回掩码；
- 日志和错误信息不能包含密钥；
- 通过单独的写入接口覆盖密钥。

### 4.3 备份数据

```text
~/Library/Application Support/CodexManager/backups/<timestamp>/
```

备份目录用于自动恢复和手动回滚。后续如果需要完整操作历史，可以再引入 SQLite；MVP 不强制引入数据库。

## 5. 后端模块划分

```text
API 层
  负责请求参数校验和统一响应

Provider 服务
  负责 Provider CRUD、模型目录和业务规则

配置服务
  负责 Codex 文件解析、生成和状态读取

启用服务
  负责备份、写入、验证和失败恢复

Provider HTTP 客户端
  负责 /v1/models 请求、超时和错误转换

密钥服务
  负责 Keychain 或本地安全存储

备份服务
  负责快照、恢复和备份清理
```

推荐继续使用 Python 实现，优先复用现有 `server.py` 中的解析、写入、备份、模型拉取和状态读取逻辑。

## 6. 前后端交互方向

前端通过 pywebview JS Bridge 调用后端。Bridge 对外提供稳定的应用能力，前端不关心 Python 内部模块和文件路径。

前端只需要围绕以下几类能力通信：

```text
读取启动数据
读取当前状态
Provider 新增 / 修改 / 删除
拉取模型
验证 Provider
启用 Provider
查询操作进度
回滚上一份配置
```

启用和回滚属于有副作用的操作，建议由后端返回操作状态，而不是让前端自行模拟延迟。前端负责展示“进行中、成功、失败、已回滚”等状态。

后端错误使用稳定的错误类型，例如：

```text
INVALID_PROVIDER
MISSING_API_KEY
DEFAULT_MODEL_NOT_ENABLED
UPSTREAM_TIMEOUT
UPSTREAM_UNAUTHORIZED
CONFIG_PARSE_ERROR
CONFIG_WRITE_ERROR
ROLLBACK_FAILED
OPERATION_IN_PROGRESS
```

## 7. 安全与可靠性原则

- 所有写操作串行执行，避免重复点击或并发写坏配置；
- 写入前必须备份；
- 使用临时文件、`fsync` 和原子替换；
- 写入后重新解析配置，确认格式有效；
- 网络请求设置连接超时和总超时；
- 后端日志不得记录 API Key；
- 默认不启动可被浏览器访问的 HTTP 服务，也不开放网络监听端口；
- 每次操作开始前重新读取磁盘状态，避免覆盖用户外部修改；
- 失败时恢复完整配置，而不是只恢复单个文件。

## 8. MVP 范围

### 包含

- Provider 增删改查；
- 当前 Codex 状态读取；
- 模型拉取、编辑和选择；
- Provider 验证；
- 配置备份；
- 安全切换；
- 自动回滚和手动回滚；
- API Key 掩码和安全存储；
- pywebview 原生窗口集成；
- 独立 macOS `.app` 打包和应用内自动启动后端。

### 不包含

- Codex 聊天和会话管理；
- 多用户和远程登录；
- 自动路由、熔断和故障转移；
- 多应用配置管理；
- Provider 市场或预设模板；
- 云端同步。

## 9. 开发阶段

### 第一阶段：真实数据接入

- 实现后端启动和配置读取；
- 前端替换模拟 Provider 数据；
- 完成 Provider CRUD。

### 第二阶段：模型与验证

- 接入真实 `/v1/models`；
- 完成模型目录合并；
- 完成 Provider 验证和错误展示。

### 第三阶段：切换与恢复

- 实现备份、原子写入和配置验证；
- 实现启用 Provider；
- 实现自动回滚和手动回滚。

### 第四阶段：桌面化与安全完善

- 接入 pywebview；
- 将前端资源内嵌到应用包，不依赖外部浏览器；
- 打通应用启动、后台任务和退出清理流程；
- 接入 macOS Keychain；
- 打包独立 `.app`；
- 补充异常退出和外部修改场景测试。

## 10. 验收标准

MVP 完成后，用户应能够：

1. 打开窗口并看到真实的当前 Provider 和模型；
2. 新增或修改一个 Provider；
3. 拉取该 Provider 的模型并选择可用模型；
4. 验证 API 地址和密钥是否可用；
5. 一键启用 Provider；
6. 在验证失败时看到明确原因，并确认原配置未被破坏；
7. 通过“回滚上一步”恢复到切换前的完整配置；
8. 在页面中始终看不到 API Key 明文；
9. 直接打开独立 `.app` 即可使用，整个过程不打开浏览器，也不需要手动启动后端。
