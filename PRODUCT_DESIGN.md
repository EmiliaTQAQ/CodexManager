# Codex 模型管理桌面窗口 — 产品设计文档（MVP）

## 1. 产品定位
一个 **cc-switch 式的 Codex 桌面管理窗口**：在一个干净的窗口里完成
「查看当前配置 → 管理多个供应商 → 一键切换 → 编辑模型信息 → 自动验证」，
让 Codex 的模型/供应商管理不再需要手改 `config.toml` 和 `models.json`。

> 本版本**不含对话/聊天界面**，专注"配置管理 + 一键切换"闭环。

## 2. 要解决的问题
- 多供应商（官方 / 中转站 / 本地）之间来回切换麻烦、易配错；
- 模型信息（Codex 下拉里能看到哪些模型、显示名、上下文等）散落在 json 里，缺乏可视化编辑；
- 切换后不确定 key 是否有效、配置是否写坏，缺少验证与回滚手段。

## 3. 设计原则
- **克制**：一个窗口、三件事（看 / 切 / 编模型），不做多余功能；
- **安全**：任何写入前自动备份，可一键回滚；key 不回显明文；
- **可验证**：切换后自动探测并给出明确结果；
- **复用**：后端沿用现有 `server.py` 已验证的逻辑（解析/写入/备份/fetch 模型），只重做界面壳。

## 4. MVP 主要功能

### 4.1 供应商管理
- 维护一组 Provider，每个包含：`名称 / base_url / wire_api / api_key / 启用模型白名单`
- 支持新增、编辑、删除

### 4.2 查看当前状态
- 顶部状态栏：从真实 `~/.codex/config.toml` 解析并显示
  「当前生效的 model + provider」，启动与切换后自动刷新

### 4.3 一键启用 / 安全切换
- 选中某 Provider 点击「启用」：
  - 自动写 `config.toml`（顶部 model/model_provider + provider 配置块）+ `auth.json`
  - 写入前自动生成时间戳备份，保留"一键回滚到上一可用配置"
- 切换后**自动验证**：轻量调用 `/v1/models`，界面显示 绿✓ / 红✗ 及原因

### 4.4 模型信息编辑
- 两个来源：从 Provider `fetch /v1/models` 自动填充；或手动增删
- 每条模型可编辑常用字段：`slug / display_name / context_window / reasoning 级别等`
- 勾选哪些模型写入 `models.json`（决定 Codex 模型下拉出现的内容）
- 输入 key 可见/可复制，但列表页只显示掩码，支持"重新输入以覆盖"

## 5. 非目标（明确不做，留给后续）
- Codex 对话/聊天界面（单独 v2 立项）
- 托盘菜单、开机自启
- 自动路由 / 多供应商熔断 fallback
- 官方订阅账号与第三方共存的深度管理
- Claude Code / Gemini 等多应用支持、preset 库、配置导入导出

## 6. 技术方案概览（MVP）
- **后端**：沿用现有 Python（解析/写入/备份/探测逻辑复用），不重写
- **窗口壳**：优先 pywebview 套本地页面成原生窗口，独立 `.app`
- **本地数据**：Provider 配置存本地 JSON（含掩码后 key）
- **写入目标**：`~/.codex/config.toml`、`~/.codex/auth.json`、`~/.codex/models.json`

## 7. 关键用户流程（MVP）
1. 打开窗口 → 看到当前生效的 model/provider
2. 在 Provider 列表选目标 → 点「启用」
3. 自动备份 → 写入配置 → 自动验证 → 绿✓ / 红✗
4. （可选）进入「模型管理」→ fetch 或手改 → 勾选 → 保存进 models.json
5. 出错时点「回滚」恢复上一可用配置

## 8. 路线图
- **MVP**：本文档 4/5/6/7 范围
- **v1.x**：模型深层元数据编辑、预设模板、导入导出
- **v2**：Codex 对话窗口 + 会话管理 + 窗口内切换（单独立项评估）
